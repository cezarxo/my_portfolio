import 'package:flutter/material.dart';

import 'colors.dart';

BoxDecoration kHeaderDecoration = BoxDecoration(
  gradient: const LinearGradient(
    colors: [
      Colors.transparent,
      CustomColor.bgLightl,
    ],
  ),
  borderRadius: BorderRadius.circular(100),
);
